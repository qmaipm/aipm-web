# dependencies
/node_modules

# next.js / build
/.next
/out
/build
next-env.d.ts
tsconfig.tsbuildinfo

# env
.env*
!.env.example

# 百度推送档案(运行状态,不入库)
/scripts/.baidu-pushed.json

# os / editor
.DS_Store
*.pem

# 非源代码资产(任务文档、归档包)
/file
*.zip

# 设计稿 / 规格文档(不参与构建)
/docs/superpowers
# Word 临时锁文件
~$*

# 视觉复验产物(截图/量测报告),不入库
tmp/
