# dependencies
/node_modules

# next.js / build
/.next
/out
/build
next-env.d.ts
tsconfig.tsbuildinfo

# env
.env*
!.env.example

# os / editor
.DS_Store
*.pem

# 非源代码资产(任务文档、归档包)
/file
*.zip

# 设计稿 / 规格文档(不参与构建)
/docs/superpowers
# Word 临时锁文件
~$*

# 视觉复验产物(截图/量测报告),不入库
tmp/

# 私有项目受理数据，严禁提交
.project-intake/
.data/
